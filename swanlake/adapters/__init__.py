"""Adapter template assets shipped with the swanlake package.

The `templates/` subdirectory holds non-Python files (shell hooks,
SKILL.md, zones.yaml example) that the adapters in
swanlake.commands.adapt copy into operator harnesses. They are
package-data, declared in pyproject.toml.
"""
